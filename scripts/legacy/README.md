# Legacy Scripts

These scripts are retained for traceability but are not part of the final
leakage-free experiment pipeline. Use the commands documented in the project
root `README.md` for the current workflow.
